{
  "name": "kadastr-game",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:yandex": "npm run build && cd dist && zip -r ../kadastr-game.zip . && cd .. && echo '\n✅ Archive created: kadastr-game.zip\n📦 Ready to upload to Yandex Games!'",
    "preview": "vite preview"
  },
  "dependencies": {
    "phaser": "^3.80.1"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
